struct bar { 
  field session struct sessid:session.id;
  field sessid;
}

struct user {
  field name text;
  field sid;
  field id int rowid;
}

struct session { 
  field user struct userid:user.id;
  field userid;
  field token;
  field id int rowid;
}

struct foo { 
  field session struct sessid:foo.id;
  field sessid;
  field id;
}
