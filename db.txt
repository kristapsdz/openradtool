struct user {
  field name text;
  field id int rowid;
  comment 
	"A regular user.
	 Here is some whitespace.";
}

struct session { 
  field user struct userid:user.id;
  field userid int comment "Associated user.";
  field altuserid:user.id int;
  field token int comment "Random cookie.";
  field id int rowid;
  comment "Authenticated session.";
}
